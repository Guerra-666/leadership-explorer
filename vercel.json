{
  "version": 2,
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist/client",
  "functions": {
    "api/ssr.ts": {
      "runtime": "edge"
    }
  },
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/ssr" }
  ]
}
